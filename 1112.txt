LLM 모델을 사용한 애플리케이션 구현

- LLM 모델은 종류가 많음. gpt-4.1 mini 사용
   ㄴ API 는 openai 모듈 사용
   ㄴ 해당 버전이 학습된 시점까지만 정확도 잇는
      응답 제공 -> 해결하기 위한 방법이 우리가 알아야 할 내용
- 해결 방법 1: 함수(function-call -> , tools_call -> 랭그래프 Agent: 코드로 함수 호출 최소화)
	    ㄴ 파이썬 내장함수, API 호출(실시간), 구글 서치엔진(실시간)
- 해결 방법 2: 벡터 DB를 이용하여 임베딩된 데이터에서 조회(RAG)
	임베딩? 이미 학습&튜닝 임베딩 모델 활용.(텍스트를 숫자 배열로 변환)
	어떤 것들을 벡터 DB로 만드나요? 
                          실시간이 아닌 API, 기존 문서파일(PDF,doc,csv..)
왜 사용할까? llm 모델이 모르는 학습이 안된 자료들로 응답을 만들때